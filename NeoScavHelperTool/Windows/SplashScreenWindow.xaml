<Window
  x:Class="NeoScavHelperTool.Views.SplashScreenWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:NeoScavHelperTool.Views"
  mc:Ignorable="d"
  Title="SplashScreen"
  WindowStartupLocation="CenterScreen"
  WindowStyle="None"
  ResizeMode="NoResize"
  Topmost="False"
  SizeToContent="WidthAndHeight"
  AllowsTransparency="True"
  IsEnabled="False"
  Background="{x:Null}"
  ShowInTaskbar="False"
  SnapsToDevicePixels="True"
  DataContext="{Binding SplashScreenViewModel, Source={StaticResource ViewModelLocator}}"
>
  <Grid x:Name="grid" HorizontalAlignment="Center" VerticalAlignment="Center">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Image x:Name="image" Stretch="None" Source="/resources/images/SplashScreen.png" />
    <ProgressBar
      Margin="0"
      Grid.Row="1"
      Height="30"
      VerticalAlignment="Center"
      Width="{Binding ActualWidth, ElementName=image, Mode=OneWay}"
      IsIndeterminate="{Binding IsIndeterminate, Mode=OneWay}"
      Value="{Binding Progress, Mode=OneWay}"
    />
    <TextBlock
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
      FontSize="20"
      FontWeight="Bold"
      Grid.Row="1"
      Text="{Binding Message, Mode=OneWay}"
    />
  </Grid>
</Window>
